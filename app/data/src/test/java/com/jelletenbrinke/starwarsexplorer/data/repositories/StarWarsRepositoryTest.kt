package com.jelletenbrinke.starwarsexplorer.data.repositories

class StarWarsRepositoryTest {

    // TODO: Implement the following test cases:
    // 1. return success: endpoint success (and cache the result)
    // 2. return success: cache hit (do not call endpoint).
    // 3. return failure: endpoint failure (does not cache the result)
}
